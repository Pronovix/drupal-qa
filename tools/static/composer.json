{
    "name": "pronovix/drupal-qa-static-code-analyses-tools",
    "description": "Drupal QA static code analyses tools",
    "license": "GPL-2.0-or-later",
    "type": "library",
    "_comment": [
        "composer/composer is a dev requirement because this library depends on code from that",
        "drupal/core is a dev requirement because this library depends on code from that"
    ],
    "require": {
        "composer/composer": "^2.0",
        "drupal/core": "^10.4.5",
        "phpstan/phpstan": "^1.8.11",
        "phpstan/phpstan-deprecation-rules": "^1.0"
    },
    "config": {
        "allow-plugins": {
            "ergebnis/composer-normalize": true
        },
        "platform": {
            "php": "8.3.0"
        }
    },
    "extra": {
        "composer-normalize": {
            "indent-size": 4,
            "indent-style": "space"
        }
    }
}
